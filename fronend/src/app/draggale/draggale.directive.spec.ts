import { DraggaleDirective } from './draggale.directive';

describe('DraggaleDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggaleDirective();
    expect(directive).toBeTruthy();
  });
});
